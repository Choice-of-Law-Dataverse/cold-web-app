{
  "scripts": {
    "setup": "cp \"$CONDUCTOR_ROOT_PATH/frontend/.env\" frontend/.env && cp \"$CONDUCTOR_ROOT_PATH/backend/.env\" backend/.env && pnpm run setup",
    "run": "(NUXT_API_BASE_URL=http://localhost:$((CONDUCTOR_PORT + 1))/api/v1 PORT=$CONDUCTOR_PORT pnpm run dev:web) & (PORT=$((CONDUCTOR_PORT + 1)) pnpm run dev:api) & wait"
  },
  "runScriptMode": "nonconcurrent"
}
